{
  "//": "Emulator-only config. Loads this app's OWN firestore.rules — the same file firebase.json deploys to teacher-corner-dev, so the tests exercise exactly what ships.",
  "firestore": {
    "rules": "firestore.rules"
  },
  "emulators": {
    "firestore": { "port": 8080, "host": "127.0.0.1" },
    "ui": { "enabled": false },
    "singleProjectMode": true
  }
}
