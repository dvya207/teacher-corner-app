{
  "name": "teacher-corner-otp-functions",
  "private": true,
  "//": "SEPARATE from ../functions, which holds an unwired email-code flow with no package.json of its own. firebase.json declares only THIS directory, so a deploy from this repo cannot pick those up.",
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
